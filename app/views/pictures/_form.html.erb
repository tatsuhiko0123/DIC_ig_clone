<%= form_with(model: @picture, local: true, url: choose_new_or_edit ) do |form| %>
  <% if @picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= @picture.errors.count %>件のエラーがあります。</h2>
      <ul>
      <% @picture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="picture_field">
    <p class="text"><%= form.label :"Image" %></p>
    <%= image_tag(@picture.image.url) if @picture.image && @picture.image.url %><br><br>
    <%= form.file_field :image %>
    <%= form.hidden_field :image_cache %>
  </div>
  <div class="picture_title">
    <p class="text"><%= form.label :タイトル %></p>
    <%= form.text_field :title, size: 30 %>
  </div>
  <div class="picture_content">
    <p class="text"><%= form.label :本文 %></p>
    <%= form.text_field :content, size: 30 %>
  </div><br>
  <%= form.submit value="投稿" %>
<% end %><br>
<span class="btn_other"><%= link_to "もどる", pictures_path %></span>